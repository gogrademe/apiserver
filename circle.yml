general:
  artifacts:
    - "build"
machine:
  environment:
    GOROOT:
  services:
    - docker
dependencies:
  cache_directories:
    - go
  pre:
    - docker version
  override:
    - docker build -t apiserver .
