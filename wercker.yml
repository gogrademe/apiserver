box: wercker/golang

build:
    steps:
        - script:
            name: go env
            code: |
                go env
        - script:
            name: install godep
            code: |
                go get github.com/tools/godep
        - script:
            name: restore deps
            code: |
                godep restore ./...
        - script:
            name: clean
            code: |
                go clean ./...
        - script:
            name: build
            code: |
                go build -o /usr/bin/GoGradeAPI main.go
